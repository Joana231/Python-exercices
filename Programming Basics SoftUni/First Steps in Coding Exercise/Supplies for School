pens = 5.80
markers = 7.20
preparat = 1.20

pens_count = int(input())
markers_count = int(input())
preparat_l = int(input())
discount = int(input()) / 100

sum = (pens * pens_count) + (markers_count * markers) + (preparat_l * preparat)
final = sum - (sum * discount)
print(final)